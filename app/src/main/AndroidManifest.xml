<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.mehdi.kelid">
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="ir.mehdi.kelid.KelidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ir.mehdi.kelid.ui.OtherMainActivity">

        </activity>
        <activity android:name="ir.mehdi.kelid.ui.AddAdverActivity" android:theme="@style/AppTheme"/>
        <activity android:name="ir.mehdi.kelid.ui.SplashActivity" android:theme="@style/AppTheme"/>
        <activity android:name="ir.mehdi.kelid.ui.SearchActivity" android:theme="@style/AppTheme"/>
        <activity android:name="ir.mehdi.kelid.ui.FilterActivity" android:theme="@style/AppTheme"/>
        <activity android:name="ir.mehdi.kelid.ui.ListActivity" android:theme="@style/AppTheme"/>
        <activity android:name="ir.mehdi.kelid.ui.CityActivity" android:theme="@style/AppTheme"/>
        <activity android:name="ir.mehdi.kelid.MainActivity" android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

    </application>

</manifest>